{
  "name": "@hyper/cli",
  "version": "0.1.0",
  "type": "module",
  "description": "Hyper CLI — init, dev, build, typecheck, env, routes.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/your-org/hyper.git"
  },
  "files": ["src", "LICENSE", "README.md"],
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./src/index.ts"
    }
  },
  "scripts": {
    "typecheck": "tsgo --noEmit || bunx tsc --noEmit -p tsconfig.json",
    "test": "bun test"
  },
  "devDependencies": {
    "@hyper/client": "workspace:*",
    "@hyper/core": "workspace:*",
    "@hyper/mcp": "workspace:*",
    "@types/bun": "1.3.1"
  },
  "engines": {
    "bun": ">=1.3.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "bin": {
    "hyper": "./src/bin.ts"
  }
}

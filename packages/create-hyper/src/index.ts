/**
 * `create-hyper` is a thin shim over `@usehyper/cli init`. The programmatic
 * surface is intentionally minimal — there is nothing to import here other
 * than the version string. For programmatic scaffolding, use
 * `@usehyper/cli`'s `runInit` directly.
 */

export const VERSION = "0.1.0"
